# 项目说明（给 Claude）

- 本仓库有两个独立方向：`01_设备与管道自动排布/`（当前主线）、`02_高斯泼溅场景重建/`（仅讨论，暂不实现）。
- **继续自动排布工作前，先读 `01_设备与管道自动排布/进度与下一步.md`**：包含已确认决定、实验结论、下一步任务与已知坑。
- 数学定义以 `01_设备与管道自动排布/自动排布数学模型-审查修订版.md` 第 12 节为准（优先级：12 > 11 > 1–10）。

## 工作约定

- 用中文交流与写文档；按点、分段呈现；公式与需求对应。
- 项目参数（K、ℓ_min、κ、净距、容差、权重、检修区是否在占地矩形内等）不写默认值，缺失即报错。
- 所有求解结果以独立校验器 `摆放原型/placement_cpsat.py: validate(..., lb="v2")` 评分；比较方法时用多个随机种子、相同时间预算，如实报告，包括没有改进的情况。
- 区分“已证明”“下界 / 差距”“启发式结果”“未实验”。
- Python 依赖装在仓库根目录 `.venv`，不要装进全局环境（ortools 会升级全局 protobuf，造成冲突）。
- 保存真实文件并给出路径；完成一个阶段后提交并推送到 GitHub（`origin/main`）。
