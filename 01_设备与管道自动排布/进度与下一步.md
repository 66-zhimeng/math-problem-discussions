# 进度与下一步（交接文档）

最后更新：2026-09-17。换电脑继续工作时先读本文件。

---

## 1. 一句话现状

数学定义已定案（主文档第 12 节）。块级摆放原型已用 CP-SAT 实现并做了两轮提速实验：**第一个解约 1 秒出现，但搜索在约 20 秒后停滞，离最优的可证明差距约 30%**。用户决定下一步改用**序列对表示 + 线性规划定位**的搜索方法（见第 4 节）。布管尚未开始。

---

## 2. 已确认的关键决定（不要再重新讨论）

| 事项 | 结论 | 出处 |
|---|---|---|
| 直管方向 | 只沿 ±X、±Y、±Z；45° 只在斜三通内部 | 主文档 12.3 |
| 对象连接 | 设备、弯头、三通之间必须经直管，长度 ≥ 变径段 + ℓ_min | 12.3 |
| 管件 | 只有 90° 弯头；三通取最大口径；自动同心变径 | 12.3 |
| 高度变化 | 不预设层标高；按实际几何计数；端口接入段不计；每条连接有上限 K | 12.4 |
| 占地 | 所有设备与管道的 XY 外接矩形面积，加长宽比上限 κ | 12.5 |
| 检修区 | 挂在设备上；检修区之间可重叠；**是否必须在占地矩形内为必填配置**，本项目取 `false`，产品说明须提示此坑 | 12.6、原型 README 第 3 节 |
| 目标 | 加权和，本项目面积、管长优先 | 12.8 |
| 参数 | K、ℓ_min、κ、净距、容差、权重等**一律不写死**，缺失即报错 | 12.1 |
| 规则配置 | 独立 JSON 文件，主要由 AI 编写，人用自然语言确认 | 约束注册机制草案第 6 节 |
| 分组 | 4 组制冷链（冷水主机 + 冷冻泵 + 冷却泵）完全复制；冷却塔成组；分组后约十几个块 | 求解算法草案 10.5 |
| 求解时间 | 无硬上限，可以一直跑，随时输出当前最好解 | 求解算法草案 10.5 |
| 语言 | 可接受 Rust；用户偏好解析 / 形式化方法 | 同上 |

---

## 3. 已完成的实验与结论

| 实验 | 位置 | 结论 |
|---|---|---|
| 解析形式 | `解析求解-可行性与矩阵形式.md` | 整体无闭式解；骨架（相对位置）固定后，占地有 max-plus 闭式解，管长为 LP |
| 形式化路线比较 | `形式化求解-其他路线比较.md` | 推荐：降维 + 精确搜索 + Benders 耦合 + 下界报差距 |
| 纯占地对比 | `对比实验/实验报告.md` | ≤10 块精确方法可证最优；20 块 CP-SAT 最好；40 块 SA 最好；解析面积下界很有效 |
| 摆放原型 | `摆放原型/README.md` 第 1–6 节 | CP-SAT + 端口、旋转、内部排法、复制组、检修区、管长 / 弯头下界 |
| 提速第一轮 | 同上第 7 节 | 可变尺寸区间 + 粗网格提示：60 s 结果略好于原模型（约 0.5%），非数量级提速；当前 LNS 最差 |
| 提速第二轮 | 同上第 8 节 | v2 下界使差距 38% → 30%；首解 12 s → 1 s；按下界删姿态删不掉任何一个；**瓶颈是搜索停滞与种子波动大** |

基准数字（合成算例，按 v2 定义评分）：`placement_fast.py` 60 秒中位数 J ≈ 2.658，3 个种子中最好 2.634。

---

## 4. 下一步任务：序列对搜索 + LP 定位

### 4.1 目标

在同一合成算例、同样的评分定义（v2）下，**比 `placement_fast.py` 更快地得到更好的 J**。对比口径：3 个以上种子，记录 5 / 10 / 20 / 60 秒时的 J 中位数。

### 4.2 方法

**表示：** 解 = (Γ⁺, Γ⁻, 每块姿态, 每个复制组的内部排法)。

- 序列对规则：i 在 j 左边 ⇔ 两个序列中 i 都在 j 前；i 在 j 下方 ⇔ Γ⁺ 中 i 在 j 后、Γ⁻ 中 i 在 j 前。
- 已知定理：任意无重叠布局压紧后都能由某个序列对表示，所以不会因为换表示而丢掉最优解（摆放部分）。

**评估一个候选（内层，精确）：**

1. 由序列对和姿态得到差分约束：x_j − x_i ≥ w_i + δ 等。
2. max-plus / 最长路得到该骨架下的最小 W*、H*（已有实现：`解析摆放示例.py`、`对比实验/benchmark.py` 中的 `sp_pack`）。
3. LP：在 W ≤ W*、H ≤ H*（可再试几个放宽比例）下最小化管长下界。
   - 两端点管网：姿态对决定弯头下界 c；c > 0 时用伸出点下界（线性 + 绝对值）；c = 0 时 LP 先用曼哈顿距离，再由校验器判断能否直连。
   - 多端点管网：伸出点坐标跨度（max − min，线性化）。
   - 可先用 SciPy HiGHS，n ≈ 12 时每次约毫秒级（需实测）；差分约束 + 绝对值的 LP 是最小费用流的对偶，之后可换成网络单纯形或 Rust 实现提速。
4. 用 `placement_cpsat.validate(..., lb="v2")` 计算最终 J，**所有方法以校验器为准**。

**外层搜索：**

- 模拟退火或禁忌搜索。邻域：交换 Γ⁺ 中两块；交换 Γ⁻ 中两块；两个序列同时交换；改一个块的旋转；改复制组的内部排法。
- 12 核并行：多进程、不同种子，定期同步当前最好解。
- 初始解：由 `placement_fast.py` 的 1 秒解推导序列对（按相对位置关系排序，检查一致性），失败则随机。

### 4.3 需要处理的细节（已知坑）

1. **固定块**（建筑立管固定在 (0,0)）：LP 中加 x = 固定值；若序列对使其不可行，LP 报不可行，该候选直接拒绝。
2. **检修区**：检修区与其他块不重叠，这不由序列对自动保证。两种处理方式：
   - (a) 在 LP 中按“检修区在块的哪一侧”追加差分约束，与序列对关系一致时加入，冲突时拒绝；
   - (b) 校验器发现冲突时给候选加罚。
   先用 (b) 快速验证，再决定是否改 (a)，并在结果中注明。
3. **`service_zones_inside_footprint`** 为 `true` 时，检修区也要进入 W、H 的计算。
4. **长宽比 κ**：按 `room_of` 补足，与现有评分一致。
5. **复制组**：组内所有块共用同一内部排法；旋转各自独立（原型 README 第 6 节第 5 条）。
6. **面积与管长的权衡**：只在 W*、H* 下优化管长，可能错过“面积稍大、管长明显更短”的解。需在几个放宽比例（例如 1.00、1.05、1.10）上分别求解，取 J 最小者。

### 4.4 完成标准

- 新脚本 `摆放原型/placement_sp.py` 与对比脚本；结果写入 `摆放原型/README.md` 第 9 节。
- 如实报告：比 `placement_fast.py` 快或好多少，或者没有更好。
- 提交并推送。

### 4.5 之后的任务（按顺序）

1. 对接用户在另一台电脑上完成的**正式拓扑 JSON 与规则 JSON 格式**：写适配器，转成原型输入，替换合成算例。
2. 块内部的局部优化：每块求 2–4 个候选内部排法，交给块级摆放选择。
3. 布管子问题：A* + 协商布线（`求解算法-讨论草案.md` 第 4 节）。
4. 摆放—布管耦合：逻辑 Benders 反馈（`形式化求解-其他路线比较.md` 路线 B）。

---

## 5. 环境（Windows）

```
# 仓库根目录
python -m venv .venv
.venv/Scripts/python -m pip install -r "01_设备与管道自动排布/对比实验/requirements.txt" matplotlib
```

- **不要把 ortools 装进全局 Python。** 它会把 protobuf 升到 6.x，和全局的 tensorflow、streamlit 冲突（2026-09-16 在原电脑上发生过，已恢复）。
- `.venv/` 已在 `.gitignore` 中。
- 运行示例：
  ```
  cd "01_设备与管道自动排布/摆放原型"
  ../../.venv/Scripts/python placement_fast.py 60 5
  ../../.venv/Scripts/python 下界对比.py 60 3
  ```

---

## 6. 摆放原型文件地图（`摆放原型/`）

| 文件 | 作用 |
|---|---|
| `算例.json` | 合成算例：12 块、18 个管网、参数（原型自拟格式） |
| `placement_cpsat.py` | 原模型；`load`、`validate(lb=v1/v2)`、`two_terminal_lb_v2`、`plot` 等共用函数都在这里 |
| `placement_fast.py` | 提速模型：可变尺寸区间、粗网格交接、坐标范围收紧、冗余下界割；`run(..., lb)` |
| `placement_lns.py` | LNS（效果最差，保留作对照） |
| `提速对比.py` / `下界对比.py` / `姿态探测.py` | 三个实验脚本，结果为同名 `*_log.txt`、`*结果.json` |
| `README.md` | 使用说明、检修区配置警告、全部实验结果 |
